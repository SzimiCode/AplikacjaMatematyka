Instalcja:
w terminalu w vs code
python -m venv venv

potem wchodzimy w tym terminalu .\venv\Scripts\Activate.ps1
a potem pip install -r requirements.txt

Dzialanie
do uruchamiania serwera w terminalu wpisujemy najpierw (WEJSCIE W TRYB VENV):

.\venv\Scripts\Activate.ps1

WEJSCIE W TRYB VENV:

source venv/bin/activate

URUCHOMIENIE SERWERA:

python manage.py runserver

no i tyle. dodalem tez launch.json do vscode, zeby uruchamiac przez run&debug, to wyzej
to jkaby nie dzialalo. 

w konsoli wyswietla sie link do serwera.

teraz zaczelo dzialac api, konkretnie questions i podpiete odpowiedzi. 
zeby modyfikowac baze danych nalezy wejsc pod adres serwera i /admin, np. http://127.0.0.1:8000/admin
logowanie przez superusera np. mateuszrozdzinski i haslo 1234
tam sie wyswietlaja modele baz danych i mozna juz je prosto modyfikowac